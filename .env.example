PORT=4000
MONGODB_URI=mongodb://localhost:27017/farm_animals
# Comma-separated allowed origins for CORS (defaults to "*" for local dev).
CORS_ORIGIN=
# Max requests per 15-minute window per IP (defaults to 1000).
RATE_LIMIT_MAX=1000
